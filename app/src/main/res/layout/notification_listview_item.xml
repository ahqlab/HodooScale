<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="activity"
            type="com.animal.scale.hodoo.activity.setting.alarmManager.AlarmItemListActivity" />

        <variable
            name="domain"
            type="com.animal.scale.hodoo.domain.AlarmItem" />

    </data>

    <LinearLayout
        android:id="@+id/switch_wrap"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/info_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:letterSpacing="0.01"
            android:text="@{domain.name}"
            android:textColor="@color/hodoo_text_light_gray"
            android:gravity="center_vertical"
            android:textSize="17sp" />

        <Switch
            android:id="@+id/notification_switch"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="right|center_vertical"/>

    </LinearLayout>
</layout>



